dimensões = (200,50) 
print('\ndimensões originais\n')
for di in dimensões:
    print(di)

dimensões = (400,20) # as tuplas não podem ser modificadas mais os valores nas variáveis podem 
print('\ndimensões alteradas\n')
for di in dimensões:
    print(di)
